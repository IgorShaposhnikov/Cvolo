# Cvolo.Compiler.Tooling

A narrow, immutable, versioned boundary assembly for standalone language tooling
(e.g. a future Language Server). It exposes the public surface described in the
LSP Boundary Extraction spec.

This file ships inside the Tooling artifact bundle under `docs/` as a nested,
auxiliary file used to prove recursive hashing of `SHA256SUMS.txt`.