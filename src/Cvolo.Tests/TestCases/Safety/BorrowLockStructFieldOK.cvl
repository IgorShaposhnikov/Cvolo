struct Point
{
	int X;
	int Y;
}

int Main() {
    var Point p = Point { X: 10, Y: 20 };
    ref px = ref p.X;
    var int vx = px;
    // px is no longer used after this line, so the borrow is released.
    ref py = ref p.Y;
    var int vy = py;
    return 0;
}
