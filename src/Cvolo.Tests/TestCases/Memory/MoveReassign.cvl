using System;

struct Point
{
	int X;
}

void Consume(Point p) {}

int Main() {
    var Point p1 = Point { X: 10 };
    Consume(p1); // p1 is moved (inactive)
    p1 = Point { X: 20 }; // Re-initialize (active again!)
    Console.WriteLine($"{p1.X}");
    return 0;
}
