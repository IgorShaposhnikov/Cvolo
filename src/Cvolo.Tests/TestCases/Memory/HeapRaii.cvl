using System;

struct Point
{
	int X;
	int Y;
}

int Main() {
    // Allocates on the heap. Compiler automatically inserts 'free' on exit!
    var Point p = heap Point { X: 100, Y: 200 };
    Console.WriteLine(p.X);
    return 0;
}
