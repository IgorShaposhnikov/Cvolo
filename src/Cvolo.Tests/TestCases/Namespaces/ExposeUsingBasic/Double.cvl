/// <summary>
/// Sub-namespace providing math functions.
/// </summary>
namespace MathLib.Double;

double Sqrt(double x) {
    var double guess = x;
    for (int i = 0; i < 60; i++) {
        guess = (guess + x / guess) * 0.5;
    }
    return guess;
}