/// <summary>
/// Rejection: Exposing a generic type instantiation on a public API boundary whose
/// type argument has a more restrictive visibility triggers CVL1038.
/// </summary>
private struct Secret {
    int X;
}

public struct Box<T> {
    T V;
}

// LEAK: Public function exposes 'Box<Secret>' where 'Secret' is private to this file
public Box<Secret> ExportBox() {
    var Box<Secret> b;
    return b;
}

int main() {
    return 0;
}
