using System;

void PrintLen(int[] s) {
    Console.WriteLine(s.Length);
}

int Main() {
    // Initialize the array so the Flow Analyzer knows it is safe to use!
    int[5] arr = { 1, 2, 3, 4, 5 }; 
    
    PrintLen(arr); 
    return 0;
}
