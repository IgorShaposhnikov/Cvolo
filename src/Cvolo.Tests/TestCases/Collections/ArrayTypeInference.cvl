using System;

int main() 
{
    // Test Type-Inferred Array Initialization (no explicit type)
    var arr = { 100, 200, 300 };

    var int x = arr[0];
    var int y = arr[1];
    var int z = arr[2];

    Console.WriteLine($"Inferred Array: {x},{y},{z}");
    return 0;
}
