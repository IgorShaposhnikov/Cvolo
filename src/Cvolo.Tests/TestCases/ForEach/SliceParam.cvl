using System;

void Sum(int[] s)
{
    var total = 0;
    foreach (val item in s) {
        total = total + item;
    }
    Console.Write(total);
}

int Main()
{
    int[4] arr = { 5, 6, 7, 8 };
    Sum(arr);
    return 0;
}