unsafe "C" delegate int UnaryProc(int value);

unsafe "C" int Twice(int value)
{
    return value * 2;
}

int main()
{
    unsafe
    {
        UnaryProc proc = &Twice;
        if (proc(21) != 42)
        {
            return 1;
        }
    }

    return 0;
}
